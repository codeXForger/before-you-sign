# Before You Sign — no environment variables are required.
#
# The app is BYOK: the user supplies their Anthropic API key in the browser
# and it is sent per-request via the Authorization header. The server never
# reads from env for the key, and nothing is persisted.
